# Placeholder — UI Tester will implement this page.
# Expected: username/password form → POST /auth/login → store JWT in st.session_state
import streamlit as st

st.title("SentinelRAG — Login")
st.info("Login page — implementation owned by UI Tester agent.")
